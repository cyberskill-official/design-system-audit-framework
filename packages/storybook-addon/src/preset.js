export function managerEntries(entry = []) {
  return [...entry, "@dsaf/storybook-addon/manager"];
}

export function previewAnnotations(entry = []) {
  return [...entry, "@dsaf/storybook-addon/preview"];
}
